// Re-exported from platform — do not duplicate logic here.
// This shim keeps existing imports inside excel-shell working.
export { useAccessStore } from '../../../platform/access-control/useAccessStore'
export type { AccessMode } from '../../../platform/types'
